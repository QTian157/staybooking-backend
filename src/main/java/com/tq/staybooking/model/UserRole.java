package com.tq.staybooking.model;

/**
 * since we mentioned we’ll support two kinds of users in the application,
 * we need to add ROLE_HOST and ROLE_GUEST here.
 */

public enum UserRole {
    ROLE_HOST, ROLE_GUEST
}
